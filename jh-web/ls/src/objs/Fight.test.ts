import {describe, it, expect,} from 'vitest';

export default {};

describe('Fight', () => {
    it("doFighting 平局", () => {
        try {

        } catch (e) {
            console.log(e)
        }
    })
    it("doFighting player 胜利", () => {
        try {

        } catch (e) {
            console.log(e)
        }
    })
});
